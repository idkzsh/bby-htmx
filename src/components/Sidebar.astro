---
const {obj} = Astro.props
---

<div class="flex flex-col w-40 sticky left-0 bg-background h-auto">
  {
    Object.entries(obj).map(([key, val]) => (
      <div class="flex h-16 border-r border-b text-xs font-bold items-center px-2">
        {key}
      </div>
    ))
  }
</div>
