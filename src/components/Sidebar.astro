---
const {obj} = Astro.props
---

<div class="flex flex-col w-[160px] sticky left-0 bg-background h-auto z-10">
  <div class="h-4 border-b border-r w-[160px]"></div>
  {
    Object.entries(obj).map(([key, val]) => (
      <div class="flex h-10 border-r border-b text-xs font-bold items-center px-2 w-[160px]">
        {key}
      </div>
    ))
  }
</div>
